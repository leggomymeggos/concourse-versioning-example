---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: bravissimolabs/alpine-git

inputs:
- name: source-code
- name: version

params:
  ssh_key: ssh-key

run:
  path: source-code/tag-prerelease/run.sh